[pytest]
testpaths = tests
# Make `app` importable when pytest is invoked as `pytest` (rather than
# `python -m pytest`) from this directory - the project is run from source,
# not installed as a package, so nothing else puts backend/ on sys.path.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
